import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles(theme => ({
  formControl: {
    width: '100%',
    marginTop: 20,
    marginBottom: 10
  },
  selectField: {
    padding: '10px 0'
  }
}))

export default useStyles
